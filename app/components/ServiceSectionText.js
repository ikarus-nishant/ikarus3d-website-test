import React from "react";

const ServiceSectionText = (props) => {
  return (
    <div className="text-white opacity-[0.7] text-[16px] leading-[24px] xl:text-[18px] lg:font-light">
      {props.text}
    </div>
  );
};

export default ServiceSectionText;
